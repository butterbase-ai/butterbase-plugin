---
description: "Configure OAuth providers and optional auth hooks per the plan."
---

Invoke the `butterbase:journey-auth` skill via the Skill tool, passing any arguments the user provided after the command.
