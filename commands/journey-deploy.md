---
description: "Smoke-test the deployed app: frontend reachability, function 2xx, auth round-trip, log scan."
---

Invoke the `butterbase:journey-deploy` skill via the Skill tool, passing any arguments the user provided after the command.
