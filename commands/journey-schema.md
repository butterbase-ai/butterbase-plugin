---
description: "Apply the Tables section of the plan via schema-design (with hackathon-mode RLS fold)."
---

Invoke the `butterbase-skills:journey-schema` skill via the Skill tool, passing any arguments the user provided after the command.
