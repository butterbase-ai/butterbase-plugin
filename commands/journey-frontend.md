---
description: "Scaffold/deploy the frontend (Vite/Next/static) per the plan."
---

Invoke the `butterbase:journey-frontend` skill via the Skill tool, passing any arguments the user provided after the command.
