---
description: "Implement and deploy each function in the plan; smoke each with invoke_function."
---

Invoke the `butterbase:journey-functions` skill via the Skill tool, passing any arguments the user provided after the command.
